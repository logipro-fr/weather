#!/bin/sh

cmd=`docker container ls | grep weather-php`
status=$?
if [ $status = 0 ]
then
docker exec \
    -ti \
    -e APP_ENV=test \
    weather-php php $@
else
## execution de PHP de manière autonome (pas besoin d'un environnement de containers)
docker run \
    --init \
    --rm \
    -ti \
    --name php-weather-running-script \
    -v "$(pwd)/docker/php/php.ini:/usr/local/etc/php/php.ini" \
    -v "$PWD":"$PWD" \
    --user 1000:1000 \
    -w "$PWD" \
    weather-php php $@
fi

